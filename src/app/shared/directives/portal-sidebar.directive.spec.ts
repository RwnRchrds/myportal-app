import { PortalSidebarDirective } from './portal-sidebar.directive';

describe('PortalSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
